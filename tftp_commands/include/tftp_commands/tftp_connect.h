#include "tftp_commands/tftp_sock_init.h"
#include "tftp_commands/validator.h"
#include "tftp_commands/tftp_helper.h"
#include "tftp_commands/messages.h"

#if !defined(TFTP_CONNECT_H)
#define TFTP_CONNECT_H

void tftp_connect (int argc, char **argv);

#endif // TFTP_CONNECT_H
